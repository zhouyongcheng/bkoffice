define(['angular', 'lodash'], function(angular, _) {

    var validates = angular.module('atm.validates', []);


    validates.directive('ensureUnique', ['$http', function($http) {
        return {

        };
    }]);
});
